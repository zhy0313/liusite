"""
这是users的urls
"""
from django.conf.urls import url,include
from django.contrib import admin
from django.contrib.auth.views import froms
from . import views
urlpatterns = [
    url(r'^$', views.index, name='index'),
    usr(r'login', login , {'template_name': 'users/login.html'}, name='login'),
]
